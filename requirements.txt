# ============================================================================
# Enhanced Document Converter - Core Dependencies
# ============================================================================
# For OPTIONAL MarkItDown features (audio, YouTube), see requirements-optional.txt
# For DEVELOPMENT dependencies (pytest, linters), see requirements-dev.txt
# ============================================================================

# Core: MarkItDown with all format support
# Transitively installs: beautifulsoup4, requests, lxml, mammoth,
# pdfminer-six, python-pptx, openpyxl, xlrd, Pillow, and more
markitdown[all]>=0.1.5

# Core: Mistral AI SDK for OCR, QnA, and Batch API
mistralai>=1.12.0

# Core: Load .env configuration
python-dotenv>=1.0.0

# PDF table extraction beyond MarkItDown's built-in support
pdfplumber>=0.10.0
camelot-py[cv]>=0.11.0

# PDF to image conversion (requires Poppler system binary)
pdf2image>=1.16.0

# Async file I/O for concurrent batch processing
aiofiles>=23.0.0

# System resource monitoring
psutil>=5.9.0

# Table formatting for console output
tabulate>=0.9.0
